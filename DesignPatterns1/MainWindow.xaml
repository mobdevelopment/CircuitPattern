<Window x:Class="DesignPatterns1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesignPatterns1"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="600">
    
    <DockPanel>
        <Menu DockPanel.Dock="Top" IsMainMenu="True">
            <MenuItem Header="_File" Click="OpenFile"/>
            <MenuItem Header="_Exit" Click="ExitProgram"/>

        </Menu>
        <Grid HorizontalAlignment="Stretch" ShowGridLines="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            
            <Label Grid.Row="0" Grid.Column="0" Content="In" />
            <Label Grid.Row="0" Grid.Column="1" Content="Node" />
            <Label Grid.Row="0" Grid.Column="2" Content="Out" />
        </Grid>
        <TextBox AcceptsReturn="True" />

    </DockPanel>
</Window>
